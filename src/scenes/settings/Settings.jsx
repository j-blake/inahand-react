import React from 'react';
import Typography from '@material-ui/core/Typography';

export default function Settings() {
  return (
    <div>
      <Typography variant="h4">Settings Thing</Typography>
    </div>
  );
}
