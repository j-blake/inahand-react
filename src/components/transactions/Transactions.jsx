import React from "react";
import Typography from "@material-ui/core/Typography";

export default function Transactions(props) {
  return (
    <div>
      <Typography variant="h4">Transactions Thing</Typography>
    </div>
  );
}
